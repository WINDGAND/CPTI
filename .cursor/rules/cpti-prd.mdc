---
description: 本仓库开发前必读 cpti_prd.md（CPTI 产品需求文档）
alwaysApply: true
---

# CPTI PRD 优先（`cpti_prd.md`）

在本仓库内进行**任何**开发、重构、调试、架构选型或与产品/交互相关的任务时：

1. **任务开始前**：先阅读仓库根目录的 `cpti_prd.md`，对齐产品范围、用户流程、功能模块、技术栈与部署约束（含 V1.0 MVP 与后续双人版预留等表述）。
2. **实现与决策**：以 PRD 为权威依据；若代码或方案与 PRD 不一致，应显式说明差异，并默认按 PRD 修正实现（除非当前对话里用户明确要求偏离 PRD）。
3. **文档同步**：若用户要求的产品变更会改变 PRD 中的约定，在完成改动后提醒用户更新 `cpti_prd.md`，避免文档与实现长期脱节。

路径：`cpti_prd.md`（与 `.cursor/rules/` 同仓库根目录）。
